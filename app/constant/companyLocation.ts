import type { FilterOption } from "~/components/common/DynamicFilterBuilder";

export const companyLocationFilterOptions: FilterOption[] = [
    {
      id: "name",
      placeholder: "Account Name",
      type: "text",
    }
  ];